/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['www.washingtontimes.com', 'twt-thumbs.washtimes.com'],
  },
}

module.exports = nextConfig
